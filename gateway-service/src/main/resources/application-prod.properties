# ── Producción: Gateway Config Server ─────────────────────────────────────────
# El perfil 'prod' incluye 'native' vía spring.profiles.group.prod en
# application.properties.
spring.cloud.config.server.native.search-locations=classpath:/config-repo
